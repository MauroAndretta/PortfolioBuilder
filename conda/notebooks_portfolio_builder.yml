name: portfolio_builder
channels:
  - defaults
dependencies:
  - python=3.11
  - pip
  - pip:
    - yfinance
    - requests_cache 
    - requests 
    - requests_ratelimiter 
    - pyrate_limiter
    - openpyxl
    - pyprind
    - seaborn
